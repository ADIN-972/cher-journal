{
  "name": "cher-journal",
  "version": "1.0.0",
  "private": true,
  "description": "Cher Journal - Story reading platform with monetization",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "npm run sync:translations",
    "dev": "npm run dev --workspaces --if-present --host",
    "dev:backend": "npm run dev -w apps/backend",
    "dev:admin": "npm run sync:translations && npm run dev -w apps/admin --host",
    "dev:web": "npm run sync:translations && npm run dev -w apps/web --host",
    "dev:mobile": "npm run dev -w apps/mobile --host",
    "prebuild": "npm run sync:translations",
    "build": "npm run build --workspaces --if-present",
    "build:backend": "npm run sync:translations && npm run build -w apps/backend",
    "build:admin": "npm run sync:translations && npm run build -w apps/admin",
    "build:web": "npm run sync:translations && npm run build -w apps/web",
    "test": "npm run test -w apps/test",
    "prisma:generate": "npm run prisma:generate -w apps/backend",
    "prisma:migrate": "npm run prisma:migrate -w apps/backend",
    "prisma:migrate:dev": "npm run prisma:migrate:dev -w apps/backend",
    "prisma:seed": "npm run prisma:seed -w apps/backend",
    "prisma:studio": "npm run prisma:studio -w apps/backend",
    "api:sync": "tsx scripts/sync-api.ts",
    "api:generate": "tsx scripts/generate-routes.ts",
    "api:security": "tsx scripts/api-inventory.ts",
    "admin:check-menus": "tsx scripts/check-context-menus.ts",
    "verify:api": "tsx verify-api-calls.ts",
    "verify:translations": "tsx verify-translations.ts",
    "sync:translations": "tsx scripts/sync-translations.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "chalk": "^5.6.2",
    "glob": "^10.3.10",
    "node-fetch": "^3.3.2",
    "prettier": "^3.7.4",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "dependencies": {
    "react-native": "0.81.5",
    "react-native-web": "^0.21.0"
  }
}
